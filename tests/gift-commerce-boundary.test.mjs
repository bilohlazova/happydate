import test from "node:test";import assert from "node:assert/strict";import{access,readFile}from"node:fs/promises";import{constants}from"node:fs";
const start=new URL("../src/app/gift/start/StartPageContent.tsx",import.meta.url);const api=new URL("../src/app/api/notify-gift/route.ts",import.meta.url);
test("Gift AI keeps advice and saving but removes the retired concierge form",async()=>{const s=await readFile(start,"utf8");assert.match(s,/requestGiftRecommendations/);assert.match(s,/createPersonGiftIdea/);assert.match(s,/ComingSoonNotice/);assert.doesNotMatch(s,/submitLegacyGiftRequest|gift-request-form|splitPayment|whatsAppHref|delivery: "kurier"/)});
test("legacy gift request persistence stays absent and notify endpoint fails closed",async()=>{await assert.rejects(access(new URL("../src/lib/gifts/giftRequestCompatibility.ts",import.meta.url),constants.F_OK));const s=await readFile(api,"utf8");assert.match(s,/status: 410/);assert.doesNotMatch(s,/Resend|req(?:uest)?\.json\(|gift_requests|emails\.send/)});
